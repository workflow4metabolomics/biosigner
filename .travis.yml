language: r
cache: packages

before_install:
 - R -e "install.packages(c('batch'), dependencies = TRUE, repos = 'https://cloud.r-project.org/')"
 - R -e 'source("http://bioconductor.org/biocLite.R") ; biocLite("biosigner")'
 - sudo apt-get install -y python-virtualenv

script:
 - make test
 - make planemolint
 - make planemotest
